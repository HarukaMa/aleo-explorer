{% from "macros.jinja2" import htmx_title %}

    <div class="content">
        <h1>404</h1>
        <p>can't find what you are looking for</p>
        <pre>{{ exc.detail }}</pre>
    </div>
{% if "PLAUSIBLE" | get_env %}
    <script>
        document.addEventListener('DOMContentLoaded', function () { plausible('404', { props: { path: prepareUrl() } }); });
    </script>
{% endif %}

{{ htmx_title(request, '404') }}